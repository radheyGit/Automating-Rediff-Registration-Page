package com.rediff.net.regpage.libraries;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.rediff.net.regpage.utils.AppUtils;

public class AutomatingRediffRegPage_Ravina extends AppUtils {
	/***
	 * Scenario : Find Total Labels present into the page
	 * 
	 * Step 01: check all labels are displayed or not
	 * Step 02: get All Labels Text 
	 * Step 03: print all of them into console
	 * 
	 * */
	
	private static final Logger log=LoggerFactory.getLogger(AutomatingRediffRegPage_Ravina.class);
}
