package com.rediff.net.regpage.libraries;

import java.util.logging.Logger;

import com.rediff.net.regpage.utils.AppUtils;

public class AutomatingRediffRegPage_Rajat extends AppUtils {
	/***
	 * Scenario : Find Total images present into the page
	 * 
	 * Step 01: check all images are displayed or not
	 * Step 02: get src Attribute value using driver.getAttribute(); method
	 * Step 03: print all of them into console
	 * 
	 * */
	
	private static final Logger log=Logger.getLogger(AutomatingRediffRegPage_Rajat.class.getName());
}
