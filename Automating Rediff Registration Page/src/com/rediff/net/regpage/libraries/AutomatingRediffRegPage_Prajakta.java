package com.rediff.net.regpage.libraries;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.rediff.net.regpage.utils.AppUtils;

public class AutomatingRediffRegPage_Prajakta extends AppUtils {
	/***
	 * Scenario : Find Total links present into the page
	 * 
	 * Step 01: check all links are displayed or not
	 * Step 02: get link text using driver.getText; method
	 * Step 03: print all of them into console
	 * 
	 * */
	
	private static final Logger log=LoggerFactory.getLogger(AutomatingRediffRegPage_Prajakta.class);
}
